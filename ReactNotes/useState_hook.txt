1. React hook = Special function that allows functional components to use React features without writing class components (React v16.8)

(useState, useEffect, useContext, useReducer, useCallback, and more....)


2. useState() = A React hook that allows the cretaion os a stateful variable and a setter function to update its value in the virtual DOM.

const [name, setName] = useState("Initial state");

name = Initial state